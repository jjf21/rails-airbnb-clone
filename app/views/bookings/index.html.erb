<h1> Booking effectué </h1>
<% @bookings_done.each do |booking| %>
  <h2><%= booking.description %></h2>
  <p>Start date :<%= booking.start_date %></p>
  <p>End date :<%= booking.end_date %></p>
  <p>Status : <%= booking.status %></p>
  <p>Personne qui reserve : <%= User.find(booking.user_id).email %></p>
  <%= link_to 'Edit', edit_booking_path(booking) %> | 
  <%= link_to 'Delete', booking_path(booking), method: :delete %>
  <br>
<% end %>

<h1> Booking reçu </h1>
<br>
<% @bookings_receive.each do |booking| %>
  <h2><%= booking.description %></h2>
  <p>Start date :<%= booking.start_date %></p>
  <p>End date :<%= booking.end_date %></p>
  <p>Status : <%= booking.status %></p>
  <p>Personne qui reserve : <%= User.find(booking.user_id).email %></p>
  <%= link_to 'Edit', edit_booking_path(booking) %> | 
  <%= link_to 'Delete', booking_path(booking), method: :delete %>
  <br>
<% end %>